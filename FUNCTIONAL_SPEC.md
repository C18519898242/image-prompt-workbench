# Image Prompt Workbench 功能文档

## 1. 项目定位

Image Prompt Workbench 是一个面向个人使用的本地 AI 生图工具。

核心用途：

- 浏览提示词卡片
- 查看示例图片和提示词
- 点击卡片自动填入提示词
- 可选上传参考图片
- 调用后台 Banana/Gemini API 生成图片
- 将生成结果保存到本地文件夹

本项目是一个轻量的本地工具，不以完整的在线 AI 创作平台为目标。

## 2. 参考项目和产品

### 2.1 提示词内容参考

- [YouMind-OpenLab/awesome-nano-banana-pro-prompts](https://github.com/YouMind-OpenLab/awesome-nano-banana-pro-prompts)

该项目用于参考 Nano Banana Pro 的提示词组织方式、提示词分类、示例图片和提示词卡片内容。它是提示词内容库，不作为本项目的代码依赖。

### 2.2 产品交互参考

- [YouMind Nano Banana Pro Prompts Explore](https://youmind.com/zh-CN/nano-banana-pro-prompts/explore?sortBy=views&sortOrder=desc)

该页面用于参考提示词瀑布流、示例图片、提示词展示、分类入口、排序入口和“立即尝试”类交互。本项目只参考交互思路，不复制其完整网站代码，也不依赖 YouMind 服务。

## 3. MVP 功能范围

### 3.1 提示词卡片

每张提示词卡片包含：

- 示例图片
- 提示词内容
- 标题
- 作者
- 分类
- 排序信息
- “立即尝试”按钮

点击“立即尝试”后：

1. 打开或定位到生成区域。
2. 将该卡片的完整提示词填入提示词输入框。
3. 用户可以继续编辑提示词。
4. 用户可以选择上传参考图片。
5. 用户点击生成按钮调用后台 API。

提示词卡片的数据保存在 SQLite 中。点击卡片时只填充输入框，不修改数据库中的原始提示词。

### 3.2 参考图片

- 参考图片为可选功能。
- 不上传参考图片时，支持直接使用提示词生成图片。
- 上传参考图片时，将图片与提示词一起发送给 Banana/Gemini API。
- 参考图片直接保存到本地文件夹。
- 支持的图片格式和数量由 API 适配层决定。

### 3.3 图片生成

后台生成流程：

1. 接收提示词。
2. 接收可选的参考图片。
3. 组装 Banana/Gemini 请求。
4. 调用生图接口。
5. 解析接口返回的图片数据。
6. 将生成图片保存到本地文件夹。
7. 将生成结果路径和状态写入 SQLite。
8. 返回生成结果给前端展示。

API 请求、图片编码、响应解析和错误处理逻辑可以参考已有的 `st-persona-generator` 实现，但本项目不引用、不依赖、不导入 `st-persona-generator` 的现有类或模块。新项目需要独立实现自己的 API 适配层。

## 4. 数据和文件存储

### 4.1 数据库

使用 SQLite 保存结构化数据，不使用 JSON 作为主数据库。

SQLite 至少保存以下内容：

- 提示词卡片信息
- 提示词分类
- 排序值
- 示例图片相对路径
- 生成记录
- 参考图片相对路径
- 生成结果相对路径
- 生成时间
- 生成状态
- 错误信息

### 4.2 本地文件夹

建议使用以下目录结构：

```text
data/
├─ app.db
├─ prompt-images/
├─ reference-images/
└─ generated-images/
```

SQLite 只保存文件的相对路径，不保存图片二进制内容。

## 5. 分类、搜索和排序

界面可以保留分类、搜索和排序入口，用于保持后续扩展空间。

MVP 阶段暂不要求实现完整功能：

- 可以显示分类按钮、搜索框和排序按钮的界面。
- 暂不要求分类筛选真正生效。
- 暂不要求搜索功能真正生效。
- 暂不要求排序功能真正生效。
- MVP 可以先展示全部提示词卡片。

相关字段提前保存在 SQLite 中，后续再实现具体逻辑。

## 6. API 配置

不制作 API 设置页面。

API 配置统一放在项目根目录的 `.env` 文件中，包括：

- API Key
- API Base URL
- 模型名称
- 必要的请求参数

API Key 只能由后台读取，不能暴露给浏览器端。

`.env` 必须加入 `.gitignore`，不得提交到 Git 仓库。

## 7. MVP 不实现的功能

说明：当前脚手架实现“单共享密码”的访问门禁和 Docker Compose 后端；宿主机 Nginx 的同域名静态文件与 API 转发方案只作为设计文档交接给服务器上的 AI，不由本仓库实际配置。这不是用户账户系统，也不改变下面关于多用户产品能力的范围；“在线部署”在这里仅指提供本项目的自托管部署说明，不代表云端产品运营能力已经完成。

以下功能不属于当前 MVP：

- 用户注册、个人账户登录和密码找回
- 多用户权限
- 云端存储
- 云端托管、自动发布和在线运维系统
- 计费和额度系统
- API 设置界面
- 工作流编辑器
- ComfyUI 集成
- Agent 系统
- 多模型管理界面
- 图片编辑器
- 社区分享
- 收藏、点赞和评论
- 完整的分类、搜索和排序逻辑
- 移动端适配

## 8. MVP 验收标准

满足以下条件即可认为 MVP 完成：

1. 项目可以在本地启动。
2. 可以从 SQLite 读取提示词卡片。
3. 可以显示示例图片和提示词。
4. 点击“立即尝试”后，提示词可以自动填入输入框。
5. 可以选择上传参考图片。
6. 后台可以从 `.env` 读取 API 配置。
7. 后台可以调用 Banana/Gemini 生图接口。
8. 可以解析接口返回的图片。
9. 生成图片可以保存到本地文件夹。
10. 前端可以显示生成结果。
11. 生成记录可以写入 SQLite。
12. API 失败时可以显示错误信息。

## 9. 实现原则

- 只面向个人本地使用。
- 优先保证核心生图流程跑通。
- 使用 SQLite 管理结构化数据。
- 使用本地文件夹保存所有图片。
- API 配置只通过 `.env` 管理。
- 不增加账号、权限、计费等产品化功能。
- 不依赖 `st-persona-generator`。
- 只参考其 Banana/Gemini API 适配实现。
- 先保留分类、搜索和排序的界面及数据字段，功能后续再实现。
