# Grok 图片生成功能回滚设计

## 目标

完整撤销从 `7c050b4` 到 `272091c` 的七个连续 Grok 相关提交，使项目恢复为只支持 Nano Banana 2 / Gemini 图片生成的状态。

## 回滚方式

使用一次新的反向提交撤销以下提交，保留已有 Git 历史，不改写 `main` 分支：

- `7c050b4`：Grok 图片生成设计文档。
- `083a886`：Grok 图片生成实施计划。
- `2541a4e`：共享图片生成类型与 Grok 配置。
- `b1c4ddc`：Grok 图片生成客户端。
- `92f3789`：生成接口中的 Grok 模型分派。
- `fcb5896`：生成工作台中的 Grok 模型选项。
- `272091c`：README 中的 Grok 配置说明。

执行时先用 `git revert --no-commit c151e04..272091c` 将反向变更放入工作区，再检查差异并创建一个中文回滚提交。这样可以用一个提交审阅和恢复整个回滚。

## 保留范围

- 保留 Grok 引入前的 Nano Banana 2 / Gemini 生图能力。
- 保留生成工作台、参考图、比例、分辨率、思考级别和生成历史功能。
- 不修改 `.idea/`、运行日志、测试临时目录、输出图片等未跟踪文件。
- 不执行硬重置、强制推送或文件清理。

## 验证

回滚后执行后端完整测试、前端完整测试和前端生产构建，并确认：

- 前端不再出现 Grok Imagine 选项。
- 后端不再包含 Grok 配置、路由分派和客户端。
- Nano Banana 2 / Gemini 生成请求与现有测试保持正常。
- Git 差异只包含 Grok 回滚及本设计记录。

